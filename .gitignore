# 模型缓存（约500MB，不入库）
hf-cache/

# Python
__pycache__/
*.pyc

# 测试音频片段（可用 ffmpeg 重新生成）
clip*.wav

# 系统文件
Thumbs.db
Desktop.ini

# IDE
.idea/
.dsh-uploads/

# 会话恢复备份（.recovery/ 下的文件为恢复用副本，不入库）
.recovery/

# 便携打包产物（可随时重新生成）
build_portable/
dist/

# 本地私密/开发杂项（不入库；保留本机工作流，公开仓库不应包含）
项目复盘记录.md
experiments/
demo_stream.py
transcribe.py
启动字幕服务.bat
启动字幕服务.vbs

# 第三方 vendored 二进制（约 23MB，不入库；克隆后运行 tools/fetch_vendor.py 获取）
extension/transformers.js
extension/ort/

# 开发布局下的自动拉起产物（本机专用，不入库）
# engine 是指向 conda 环境 bilibili_subtitle 的目录 junction（约 471MB）；
# subtitle-native.exe 由 tools/native_host.cs 编译得到；
# native-manifest.json 含本机绝对路径，由注册脚本生成。
engine/
subtitle-native.exe
native-manifest.json
native-host-debug.log

